<project name="BrainFarmFlexLib.assets">
    <dirname property="BrainFarmFlexLib.assets.dir" file="${ant.file.BrainFarmFlexLib.assets}"/>

    <!-- Define SWC exported by this library. -->
    <property name="brainfarm.flex.core.swc" location="${brainfarm-flex-core.assets.dir}/${build.mxmlc.dir}/${project.swc}"/>

</project>
